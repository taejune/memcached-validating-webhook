apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: memcached-webhook-cert
spec:
  secretName: memcached-validating-webhook-certs
  dnsNames:
  - memcached-validating-webhook-svc
  issuerRef:
    name: ca-issuer